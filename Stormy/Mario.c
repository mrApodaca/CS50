#include <cs50.h>
#include <stdio.h>
#include <math.h>

int main(void)
{
       
        printf("How tall would you like the half pyramid to be between 0-23:");
        
           int n = GetInt();
           
           for (int i = 0; i < n; i++)
           {   
                   // pirntf spaces
                   // printf hashes
                   // new line
                //code outside
           }
           
         
}
