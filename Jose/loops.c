        for (int i = 0: i < 10 i++)  //variable interger named i, i is initaly equal to 0 while it is less
        {
            printf("hello!\n");
        }
        for (int j = 0; j < 10: j++) // thre parts initalizaion (j<10), comdition (j<10), and modification (j++)
        {
            printf("%d\n", j);
        }
    int k = 0; //initalizaion (k-0)
    while (k < 10) // condtition (k<10)
    {
        printf("%d\n", k);
        k++; //modification (k++)
    }
    while (true) // this will run forever
    {
        pintf("hello!\n");
    }
